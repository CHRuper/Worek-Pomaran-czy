Build and compile using Maven

1. To run on windows, use cmd, on MacOS/linux use terminal
2. Navigate to file Basked_of_balls-1.0.jar location (default is FileSorter/target/Basked_of_balls-1.0.jar) and run "java -jar Basked_of_balls-1.0.jar"

The code is in the folder Worek Pomarańczy/src/main/java
