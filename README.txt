Build and compile using Maven

1. To run on windows, use cmd, on MacOS/linux use terminal
2. Navigate to file FileSorter-1.0-SNAPSHOT.jar location (default is FileSorter/target/FileSorter-1.0-SNAPSHOT.jar) and run "java -jar FileSorter-1.0-SNAPSHOT.jar"

The code is in the folder Worek Pomarańczy/src/main/java